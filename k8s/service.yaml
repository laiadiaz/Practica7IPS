apiVersion: v1
kind: Service
metadata:
  name: springuma-service
  namespace: default
  labels:
    app: springuma
spec:
  type: NodePort    
  selector:
    app: springuma        
  ports:
    - protocol: TCP
      port: 8080          
      targetPort: 8080     
      nodePort: 30080   

